
import StationPageClient from '@/components/admin/StationPageClient';

export const metadata = {
  title: 'Data Entry: Reaction Time',
};

export default function ReactionStationPage() {
    return <StationPageClient stationKey="reaction" />;
}
