
import StationPageClient from '@/components/admin/StationPageClient';

export const metadata = {
  title: 'Data Entry: Push Ups',
};

export default function PushupsStationPage() {
    return <StationPageClient stationKey="pushups" />;
}
