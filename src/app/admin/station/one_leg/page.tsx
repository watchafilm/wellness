
import StationPageClient from '@/components/admin/StationPageClient';

export const metadata = {
  title: 'Data Entry: One Leg Stand',
};

export default function OneLegStationPage() {
    return <StationPageClient stationKey="one_leg" />;
}
